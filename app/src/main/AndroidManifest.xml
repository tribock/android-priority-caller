<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Read the address book to pick the priority contact -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Needed to detect when a ringing priority call is answered or ends -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Required to read/override Do Not Disturb policy -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Foreground service that plays the ringtone while a priority call is ringing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets the app draw a full-screen "priority call" alert over the lock screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PriorityCaller"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The system binds to this to let us screen every incoming call.
             Requires the app to be granted the CALL_SCREENING role at runtime
             (handled in MainActivity via RoleManager). -->
        <service
            android:name=".CallScreeningServiceImpl"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Foreground service: plays the looping ringtone at max volume.
             Uses "specialUse" because this service doesn't act as a dialer/ConnectionService
             (which "phoneCall" requires, along with the MANAGE_OWN_CALLS permission we don't hold) —
             it just rings a notification/sound while a priority contact's call comes in. -->
        <service
            android:name=".RingtonePlayerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Rings a loud alert when a priority contact calls" />
        </service>

    </application>
</manifest>
